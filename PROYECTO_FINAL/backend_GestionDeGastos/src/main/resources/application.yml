spring:
  application:
    name: personal-expenses-tracker

  data:
    mongodb:
      uri: mongodb+srv://felipe123:F2025password898@cluster0.gaodo1w.mongodb.net/expensesdb?retryWrites=true&w=majority
      database: expensesdb

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: your-google-client-id
            client-secret: your-google-client-secret

server:
  port: ${PORT:8080}
  servlet:
    context-path: /

app:
  jwt:
    secret: ${JWT_SECRET:mySuperSecretKeyForJWTTokenGenerationInPersonalExpensesTrackerApplication2024}
    expiration: 86400000 # 24 horas en milisegundos

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
